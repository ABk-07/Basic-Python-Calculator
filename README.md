# 🧮 Basic Python Calculator

This is a simple interactive calculator built in Python.  
It allows users to perform basic arithmetic operations: **addition, subtraction, multiplication, and division**.

---

## ✨ Features
- Prompts the user to enter two numbers.
- Asks the user to choose an operator (`+`, `-`, `*`, `/`).
- Performs the chosen calculation and displays the result.
- Handles errors:
  - Invalid operator input.
  - Division by zero.
  - Non-numeric input (e.g., letters instead of numbers).

